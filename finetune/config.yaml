# OptimiseLora01
TTS_dataset: Jpisnice/tokenised-konkani01

model_name: "canopylabs/3b-hi-pretrain-research_release"

# Training Args
epochs: 1
batch_percent: 1
number_processes: 1
pad_token: 128263
save_steps: 5000
learning_rate: 5.0e-5

# Naming and paths
save_folder: "checkpoints"
project_name: "tuning-orpheus01"
run_name: "5e5-0"
